import LoginFormClient from './login-form-client';

/**
 * Login Page
 * Simple page that renders the login form
 * Authentication is handled entirely client-side via loginAction server action
 */
export default function LoginPage() {
  return <LoginFormClient />;
}
